class AchievementModel {
  String image;
  String title;
  String description;
  int level;
  int taskTotal;

  AchievementModel(this.image,this.title, this.description, this.level, this.taskTotal);
}